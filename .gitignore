standard-reserve-explainer-prompt.md

.vercel/
node_modules/

# Local test harnesses. Kept untracked so they stay out of the repo, and named
# in .vercelignore as well, because vercel deploy uploads the working directory
# rather than the git tree.
_phonetest.html
_rmtest.html

.vercel

# capture artefacts and tooling deps
out/
node_modules/
