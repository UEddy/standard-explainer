{
  "name": "standard-explainer-capture",
  "private": true,
  "description": "Tooling only. The site itself is static and has no build step.",
  "scripts": {
    "capture": "node capture.js"
  },
  "dependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "playwright": "^1.63.0"
  }
}
